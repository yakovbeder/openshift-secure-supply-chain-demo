apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: secure-app
  namespace: secure-app
  labels:
    app: secure-app
spec:
  to:
    kind: Service
    name: frontend
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect









