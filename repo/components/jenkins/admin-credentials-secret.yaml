apiVersion: v1
kind: Secret
metadata:
  name: jenkins-admin-credentials
  namespace: jenkins
  annotations:
    argocd.argoproj.io/sync-wave: "0"
type: Opaque
stringData:
  # Demo: admin / openshift for easy access (same as Gitea, Quay)
  JENKINS_ADMIN_USER: "admin"
  JENKINS_ADMIN_PASSWORD: "openshift"
