apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: jenkins-agent-supply-chain
  namespace: jenkins
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app: jenkins
    role: jenkins-agent
spec:
  lookupPolicy:
    local: true
